# Oprix — Build Status Snapshot

Compact current-state reference. See **`docs/REFERENCE.md`** for local setup,
architecture & design rationale, the build roadmap, and the companion-extension
design. Day-to-day extension load/use is in `extension/README.md`.
_Last updated: 2026-06-30 — **Task checklist perms + no-checklist**: every checklist item records who added it (`ChecklistItem.createdById`; creation-time items = the task creator). **Delete rule** (server-enforced + reflected in the UI): the task creator can remove any item; anyone else can remove only items they added — an assignee can't delete the task's original checklist. New-task form gains a **"No checklist for this task"** option → `Task.checklistEnabled=false`, which **hides the Checklist box** on the detail page. **Needs `prisma db push`**. Earlier same day — **Tasks group-by view**: the tasks table can group rows into collapsible sections by **status / project / department** (status in workflow order), each with a count; grouped mode shows all filtered rows (no pagination). The choice persists per-device in localStorage (`oprix:tasks-group`) until changed. Earlier same day — **Form fill-out reminders**: a form manager can schedule "please fill this out" notifications to the audience roles — **one-time / daily / weekly (by weekday) / monthly (by day-of-month)** at a set company-local **time** (Form settings in the builder). Delivery: `lib/forms/notify-cron.ts` fires via the `/api/cron` daily job **and** the lazy app-shell fallback (atomic per-day claim on `Form.notifyLastRunKey`), routed through the central `notify()` (in-app bell + Web Push) with a new **"Forms"** notification category → deep-links to the form. **Needs `prisma db push`** (`Form.notifyEnabled/notifySchedule/notifyLastRunKey`). Earlier same day — **Form entries — detail/edit modal**: clicking an entry opens a modal with the complete untruncated data (repeater rows expanded) + an **Edit** mode (manager or submitter → `updateSubmission`, server-revalidated). When grouped by a field, that column no longer repeats inside the group's rows. Earlier same day — **Trash — permanent delete + bulk**: Super-Admin can now **permanently delete** trashed items (irreversible, confirm-gated) — `permanentlyDelete` in `lib/trash/actions.ts` hard-deletes the row + its children + on-disk files in one transaction (per-type cleanup; a missed FK fails cleanly, no partial corruption; **employees are blocked** — they keep attendance/payroll history). **Bulk select** (checkboxes + select-all + toolbar) for Restore / Delete-permanently many at once. Earlier same day — **Form Builder — entries UX**: the entries view is now a real data table (**sortable headers, page-size + pagination, group-by**: by submitter for view-all roles, by any field for everyone incl. own-only viewers). **Every filler can open "My entries"** (form page + list); own-only viewers no longer get a redundant "Submitted by" column. **Deleted entries now go to the Trash** ("Form entry", Super-Admin restore). Earlier same day — **Form Builder — add-more list + drag UX**: new **Add-more list** field (`type: "list"`) — the filler adds as many entries as they want under one label via "+ Add more" (entry type text/number/email/phone/date; stored as a string array); distinct from the repeater. Builder reordering is now grab-anywhere (the whole field card is the drag handle; click still selects). Earlier same day — **Form Builder — logic**: added **conditional visibility** (`FieldDef.visibleWhen` — show a field only when another's answer equals / ≠ / contains / >, < / is-answered / is-empty; hidden fields aren't required or stored; works per-row in repeaters) and **auto-calculation** (a read-only `calculation` field with a formula: `{fieldId}` = that field's number, `{repeaterId.subId}` = column sum; safe recursive-descent evaluator, **no eval**; server recomputes authoritatively; works as a repeater sub-field for line-totals → grand-total). No `db push`. Earlier same day — **Form Builder — more field types**: added **Date range** (two linked pickers → `[start,end]`), **Dynamic list** (a dropdown whose options come live from the company's **clients/projects/employees** — `lib/forms/lookups.ts`; portal lookups are client-scoped for privacy), and **Repeater** (a row of scalar sub-fields the filler adds/removes N times; answers nest as an array of row objects). All stored in the existing JSON columns — **no `db push`**. Remaining builder work: auto-calculation + conditional logic. Earlier same day — **Form Builder (Module 12) — Phase 1 (foundation)**: a drag-and-drop builder (**dnd-kit** — the app's first UI dependency, user-approved) with 13 basic field types, **per-form access** (audience staff roles + client-portal toggle + per-role "see all entries"), a dynamic renderer + submissions, an **entries table + CSV export**, and **client-portal fill**. Form structure is JSON (`Form.schema`); answers are JSON (`FormSubmission.data`); new `form:manage` capability (Super Admin default, grantable in the Access matrix); deleted forms ride the Trash module. **Needs `prisma db push`** (Form + FormSubmission tables; prod auto-applies on redeploy). Repeater / auto-calculation / conditional logic are **Phase 2**; charts on entries, templates, entry edit, file fields, submit-notifications are **Phase 3**. Earlier 2026-06-27 — **Leave rework**: apply form moved to its own page (`/leave/apply`), split from the applied-leaves list (`/leave`); **every leave row opens a detail popup** (reason, applied-on, status); **edit-request approval workflow** — applicant (or manager) proposes a change stored in `LeaveRequest.pendingEdit` (JSON) that does NOT alter the live request until an approver **Approves**/**Rejects** it (needs `db push` for `pendingEdit` + `editRequestedAt`). Calendar **apply-leave/WFH removed** (disabled via `canApplyLeave = false`; code kept). Earlier 2026-06-17 — **Pre-launch hardening pass**: multi-assignee timer finalization, leave overlap/balance guards, overnight punch-out fix, extension CORS fail-closed + login rate-limit, deactivated-user redirect-loop fix (`/logout` route), email case-insensitivity + `User.email` sync, JWT alg pin, dead-code removal. Plus project changes: `Project.type` (one-time/recurring, recurring hides progress), create-time asset upload, 2-column detail, **Kanban board removed**, deleted-announcement page. Code-only — `tsc` green, no `db:push`. Earlier (2026-06-16 pm): **Services are now categories → sub-categories** (`Service.parentId`): projects link categories, tasks pick a sub-category (which seeds the checklist), and **task assignees are scoped to the sub-category's department** (per-service "primary assignee" removed). Task creation gained a **description** + **file attachments stored on disk** (`uploads/`, served via `/api/files/[id]`, never in the DB). Data migrated (existing services → sub-categories under per-dept "General"; project links repointed to categories). `tsc` green. Earlier same day: Companion extension + `/api/ext/v1`; Resource Allocation frontend removed._

## Stack (as built)
- **Next.js 16** (App Router, Turbopack) · **TypeScript** strict · **Tailwind v4**
- **Prisma 6.19.3** (pinned — Prisma 7 moved DB URLs to a config file) · **PostgreSQL on Supabase**
  - **DB in Mumbai (`aws-1-ap-south-1`). Runtime = SESSION pooler on port 5432; migrations = the true direct connection (`DIRECT_URL` → `db.<ref>.supabase.co:5432`).** Not the 6543 transaction pooler.
  - **Shared DB with real data → `db:push` only, never `db:seed`.**
- **Auth:** hand-rolled `jose` JWT in an httpOnly cookie (not NextAuth) + set-password invite flow. Session carries `userId, companyId, role, email, employeeId, clientId`.
- **Email:** `nodemailer` SMTP — Gmail configured in `.env` and verified sending; falls back to console logging if `SMTP_HOST` is unset.
- **UI:** custom primitives — searchable `Combobox`, themed `DatePicker`, `Modal` + right offcanvas drawer, `Avatar`, and **global `toast` + `confirmDialog`** (themed, replace native `alert()`/`confirm()` app-wide; `<Toaster/>` + `<ConfirmHost/>` mounted in the app shell). Emerald/teal palette; full light + dark theme; collapsible sidebar. Main content max width **1600px**.

## Modules live (build-verified: `tsc` green + `next build` passes)
- **Auth/RBAC** — login, logout, `proxy.ts` route guard (role-aware: CLIENT → `/portal`, staff → app), `/set-password`. Dynamic permissions (`RolePermission`, `lib/auth/permissions.ts`, **Organization → Access** matrix); Super Admin always full.
- **Dashboard** — role-aware; punch in/out card (live timer, shift grace, late flag); greeting adapts to punch state (no "punch in" prompt once the day is done); **personal widgets** — your tasks due today + latest notifications (5, with "view all"). Greeting hour/date are company-tz-aware.
- **Organization** — single **Company** tab (profile + sidebar branding; work shifts [**now editable**], locations, probation, day-before reminder), Departments, **Services (category → sub-category tree)**, Designations, **Admins** (Super-Admin-only), Access matrix, Task access.
- **Super Admin access** — **Organization → Admins** tab (Super-Admin-only; `components/org/admins-panel.tsx`, `lib/admins/*`). Grant SA to any team member with a login, or revoke (→ Employee). The **primary admin = the company's earliest-created SUPER_ADMIN** (dynamic — `getPrimaryAdminUserId`, no schema flag; for Epic that's `access@gowithepic.com`, created by `prisma/reset.ts`, which has no employee record); it's protected from removal/demotion everywhere. Roles are read fresh from the DB per request, so changes take effect on the target's next request. A category carries the department; sub-categories inherit it and hold the checklist template tasks seed from.
- **Employees** — directory, create/edit, profile, emergency contacts, email invite + resend. **Profile page work-summary widgets** — Tasks (total / to-do / in-progress / in-review / delivered, with an on-time vs delayed split), Projects (distinct projects the person is assigned tasks in), and Leave (taken/left per type via `computeBalances`); all clickable to the filtered destination **when the viewer has the capability** (tasks → `/tasks?assignee=&status=`, projects → `/projects/[id]`, leave → `/leave/requests`). The tasks page now honours `?assignee` (server filter) + `?status` (initial table filter). **Last appraisal date** (`Employee.lastAppraisalAt`) is inline-editable by HR / Super Admin (`setLastAppraisal`, `employee:manage`).
- **User profiles** — `/profile` (self) + `/people/[id]` (Active/Away badge). **Names across the app now link to `/people/[id]`** (monthly attendance, People report, task assignees, comment authors).
- **Attendance** — admin daily grid (status modal, self-log confirm) **plus a monthly register (`/attendance/monthly`)**: employees × days, status-coded (P/A/½/L/H), month nav. Self-service punch, leave sync, holiday banner, late detection → notifications.
- **Leave** — **`/leave/apply` (apply form) is a separate page from `/leave` (applied-leaves list)**; the sidebar/header "Apply for leave" points there. Types (allowance per month/year, editable); self-service apply (live balance + over-balance block, half-day **with a first-half / second-half selector** — `LeaveRequest.halfDayPeriod` "FIRST"|"SECOND", `lib/leave/half-day.ts`, shown on apply/admin-add/both edit forms + the detail popup, WFH, Employee→Manager→HR); overlap guard + balance check on self-apply and manager-create; segregation of duties on approval; notifications. **Each leave row (yours, or anyone's for managers) opens a detail popup** with the full reason, applied-on time, and status. **Edit-request approval** (`LeaveRequest.pendingEdit` JSON + `editRequestedAt`): the applicant — or a manager — proposes a change (dates/type/half-day/reason) that does **not** change the live request; an approver **Approves** (applies it, with segregation-of-duties) or **Rejects**; both sides see the pending change meanwhile. Actions: `requestLeaveEdit` / `approveLeaveEdit` / `rejectLeaveEdit`. **Working-days counting** — leave day counts exclude weekly offs, per-nth-Saturday rules, and holidays inside the range (`lib/leave/work-week.ts` + `lib/leave/count.ts`; `Company.workWeek` JSON `{workingWeekdays[0..6], saturdayOffWeeks[1..5]}`, set in **Organization → Company → Working days**, gated by `org:manage`). Applied everywhere days are computed (`applyLeave`/`createLeaveRequest`/`requestLeaveEdit`/`adminEditLeave`) + payroll LOP; an all-non-working range is rejected; apply form shows the live count. **Employees can only edit while PENDING** — once approved/rejected, `requestLeaveEdit` is blocked and the edit UI hides. **Delete** (`deleteLeaveRequest`, `leave:approve`-gated, cleans attachments) is available to approvers/Super Admin from the detail modal — never the applicant. Balances show per-type taken/left on `/leave`.
- **Calendar** — holidays, who's away, announcements; click/drag to act (**admins only**). **Applying for leave/WFH from the calendar was removed** — people apply at `/leave/apply`; the calendar's apply-leave code is kept but disabled (`canApplyLeave = false` in `app/(app)/calendar/page.tsx`). Announcement fan-out; optional day-before reminders. Announcements are **author-scoped** (`Announcement.authorId`): the poster (or a Super Admin) can edit/delete them.
- **Tasks → Calendar view** — multi-day tasks render as **continuous spanning bars** (Google-Calendar style): per-week lane packing, flat edges at week boundaries, colored by status, "Due" tag on the deadline day, "+N more" overflow.
- **Projects** — list with progress (**recurring projects hide done/total**), create (**pick service categories** + upload assets at creation), **simple task list** (the per-project Kanban/drag board was removed — it conflicted with the timer/auto-status flow), **edit modal** (name/description/priority/**type: one-time vs recurring**/dates + soft-delete), **Service-categories panel**, **Attachments** (on-disk), Deliverables panel. Detail page is a **2-column layout** below the header.
- **Tasks** — review workflow; **List / Calendar** view switcher (`tasks-workspace`); per-row edit/delete; **description + file attachments** (list/download/delete on the detail page); created under a **sub-category** with **department-scoped assignees**; inline assignees + checklist; comments (@-mentions); per-user timers; role-based visibility. `deleteTask` cleans up all child rows **and their on-disk files**. **Detail page:** redesigned header (breadcrumb + labelled meta strip + distinct description); assignees can **withdraw a submission** to resume work (`withdrawSubmission` → clears final link, back to In progress); **comment edit/delete** by the author (`editComment`/`deleteComment`); **rich-text comments** — the comment box is a compact WYSIWYG (`components/tasks/comment-editor.tsx`: bold/italic/lists/link + an image button and **clipboard image paste** — pasted/picked images show as **thumbnail chips** above the text, not inline; a **posted** comment renders its text then its images as a matching **thumbnail row** (`lib/tasks/comment-content.ts#splitCommentImages` separates text from `![](…)` images), and clicking any thumbnail opens it full-size in the `AttachmentLightbox`), reusing the KB editor's `htmlToMarkdown` + the XSS-safe `renderMarkdown` (now with allowlisted `![](…)` image support), so bodies stay Markdown and render safely; @-mentions still autocomplete. Pasted/picked images upload to `POST /api/tasks/[id]/comment-images` (gated like commenting: manager or assignee) as **`inline` Attachments** (`Attachment.inline` — served via `/api/files/[id]`, embedded as `![](…)`, and hidden from the Attachments panel). **live timer sync** — every viewer sees who's actively timing + a ticking elapsed clock (`getTaskRunningTimers` + `TaskLiveTimers`, page has `LiveRefresh`). Images/PDFs **preview in an on-page lightbox** (`AttachmentLightbox`, wired into `AttachmentGrid` + `AttachmentsPanel` site-wide) instead of opening a new tab. **Due-date timeliness** — the header shows the due date as a prominent pill with an **On time / Delayed** verdict (`TaskDueBadge`): green "On time" if submitted on/before the due date, red "Delayed" if submitted late OR if an active task (To do / In progress / Redo) is past due but still unsubmitted. Backed by a new `Task.submittedAt` (set on submit-for-review, cleared on request-changes / withdraw). Leave apply form now says "N working days". **List table (`tasks-table`)** is a wide ClickUp-style grid: horizontal scroll with **frozen Task ID (+ checkbox) left and Actions right**, **sticky header**, and columns Task ID · Task · Type · Project · Description · Assigned to · Due · Client deadline · Status · Priority · Assigned on · Department · Final link · Delivered on · Delivery status · Delayed by · Total time. **Human Task ID** = `Dept - N` from a per-company counter (`Company.taskSeq` + `Task.taskNumber`, assigned in `createTask`/`duplicateTask`); multi-word departments abbreviate to initials (Business Manager → "BM", `deptAbbrev`). Legacy rows are numbered **lazily/self-healing on the tasks page load** (creation order, guarded `updateMany` so it's race-safe) — no manual step; `prisma/backfill-task-numbers.ts` still exists for a bulk pre-number. **Total time** via batched `getTaskTotals`.
- **Recurring tasks** — a separate module (`/tasks/recurring`, under **Tasks → Recurring tasks** in the sidebar, `task:manage`-gated) that spawns tasks automatically on a schedule. A `RecurringTask` template holds the project + task type, name/description/priority, assignee ids (`Json` string[]), relative **due / client-deadline offsets in days** (from each occurrence date), a checklist toggle, and a **schedule** reusing the Form-Builder engine (`lib/forms/schedule.ts`: ONCE / DAILY / WEEKLY(weekday) / MONTHLY(monthday, clamped) at an `HH:MM`, company-tz). Firing mirrors form reminders: `lib/tasks/recurring-cron.ts#runRecurringTasks` claims today's `lastRunKey` atomically (`updateMany` guarded on the prev value) so a cron hit + a lazy page-load can't double-create; the shared creator `lib/tasks/recurring.ts#createTaskFromRecurring` builds a normal task (real Task ID via `Company.taskSeq`, seeds the checklist, notifies assignees via `notifyTaskAssigned`). **Checklist is snapshotted at setup** (`RecurringTask.checklist` Json string[]): the form seeds it from the sub-category template and lets you edit it, so every occurrence gets exactly that list (older templates with a null snapshot fall back to the live sub-category template). Wired into `runDailyJobs` (`/api/cron`) **and** the app-shell lazy fallback (`app/(app)/layout.tsx`). Actions (`lib/tasks/recurring-actions.ts`): create / pause-resume (`active`) / delete (past instances untouched) / **Run now** (spawns immediately without touching the schedule). UI (`components/tasks/recurring-tasks.tsx`) lists templates with their schedule + next-fire meta and an inline create form (edit = delete + recreate for v1). **Visibility follows the task scope** (`resolveTaskScope`): admins (ALL) see every template; everyone else sees only the ones they created or are assigned to. **All / My tasks / Assigned by me** view tabs filter the list (`?view=mine|created` deep-links).
- **Time tracking** — per-task/user stopwatch, non-destructive pause, global bottom bar.
- **Clients** — list, contacts, project mapping, **edit**, portal-access invite.
- **Payroll (Module 8)** — full v1: per-employee **salary structures**; monthly **runs (DRAFT→LOCKED→PAID)**; **PF / ESI / Professional-Tax** calculators with **company on/off toggles** (`Company.pfEnabled` / `esiEnabled`); **loss-of-pay** from approved unpaid leave (Gross ÷ 30 fixed, excl. Sundays & company holidays, half-day = 0.5); per-run **bonus/deduction adjustments**; **frozen payslip snapshots**; **printable payslips** + employee self-service `/payslips`; payslip-ready notifications on "mark paid". Pure math in `lib/payroll/{config,calc}.ts`; LOP in `lib/payroll/lop.ts`.
- **Form Builder (Module 12)** — `form:manage` users build forms in a **dnd-kit** drag-drop builder (**18 field types**: text/long-text/number/email/phone/date/**date-range**/dropdown/multi-select/radio/checkbox/yes-no/**dynamic-list (clients/projects/employees)**/**repeater**/**add-more list**/**calculation**/heading/paragraph; reorder by dragging the whole field card), with **conditional visibility** (`visibleWhen`) and **auto-calculation** (formula evaluator, server-recomputed). Set **per-form access** (audience roles + client-portal + per-role "see all entries"), Draft/Published/Closed. Anyone in the audience fills `/forms/[id]` and can open **My entries**; entries are a **data table** (search, sortable columns, pagination, **group-by** submitter/field, CSV export; own vs. all per role — own-only viewers don't see a submitter column). Clients fill portal-enabled forms at `/portal/forms`. Deleted forms **and entries** ride the Trash. Schema/answers are JSON (`lib/forms/types.ts` is the isomorphic field contract; dynamic options via `lib/forms/lookups.ts`); soft-delete → Trash. **Pending:** charts on entries, templates/duplicate, entry edit, file-upload fields, submit notifications.
- **Knowledge Base** — guides scoped Project → Department → Sub-category; each article is **written content** (hand-rolled WYSIWYG Markdown, XSS-safe) **or an external link** (`KbArticle.externalUrl`; a content-type radio picks which). Link articles **open in a new tab** from the KB list, task "Related guides", and the extension; their own page shows an "Open external resource" button. Change log; task "Related guides" card.
- **Reports** — read-only analytics: Time, Projects, People, Attendance, Leave, **Payroll (`/reports/payroll`)** (salary summary + cost by department). The reports overview doubles as a company dashboard (incl. a **Monthly payroll** KPI). Custom ranges, multi-color charts, XLS/CSV/PDF export.
- **Notifications** — topbar bell → drawer; `/notifications` filterable by category (Tasks/Mentions/Attendance/Leave/Announcements/**Payroll**/Clients/Forms/General). Events: task workflow, @-mentions, late login, leave, announcements, deliverables, reminders, **payslip ready**. **Three channels through one choke point `notify()`:** in-app bell (always), **Web Push** (all sources now route through `notify()` — Phase 2 done, so mentions/leave/workflow/payroll/portal/reminders/late-login all push in real time), and **email** (per-category opt-in). **Per-user email preferences** live on a dedicated **`/profile/notifications`** page (under the profile dropdown + a "Notification settings" button on `/notifications`); it also hosts the per-device push toggle. (`User.emailPrefs` JSON keyed by category; sensible defaults — Tasks/Mentions/Leave/Payroll on, the rest off; `emailEnabled()` in `lib/notifications/categories.ts`). A **global "enable notifications" banner + one-time login modal** (`components/notifications/push-prompt.tsx`, mounted in the app shell) checks browser permission + subscription on login and nudges opt-in; shared browser logic in `lib/push/client.ts`. Needs `VAPID_*` (push) and `SMTP_*` (email) env set.

### Client Portal (Module 10) — Phase 1 + Phase 2 built, client-side walkthrough pending
- Isolated `/portal` segment; proxy confines CLIENT to `/portal/*`; `requirePortal()`; every query scoped to `clientId`. Invites from Clients page; overview + projects (progress-only); approve/request-changes on CLIENT_REVIEW tasks + deliverables; decisions notify the team.
- **Client teams (Phase 2)** — multiple CLIENT logins per client (`Client.users`). New **`/portal/team`** page (`components/portal/team-manager.tsx`): the **primary contact** (the earliest-created login — dynamic, no flag) can invite/remove members (`inviteTeamMember` / `removeTeamMember` in `lib/portal/actions.ts`, reusing the set-password invite email); removal deactivates (`isActive=false`, re-invitable). All members view projects/deliverables and can raise tasks.
- **Business Manager (Phase 2)** — a **`Department.clientFacing`** checkbox (Organization → Departments) marks the client-liaison department. A project's BM = the **primary assignee of its client-facing category** (`lib/portal/manager.ts#getProjectManager`) — no per-client field; shown on the portal project page.
- **Two-way tasks (Phase 2)** — `Task.clientVisible`. A client **raises a task** from the portal project page (`components/portal/client-task-form.tsx` → `clientCreateTask`): a real task auto-assigned to the project's BM, `clientVisible`, **due-date-only** simplified form; the BM is notified. Internally, the new-task form has a **"Visible to client"** checkbox and the task detail has a **Visible-to-client toggle** (`setTaskClientVisible`, `components/tasks/task-client-visible.tsx`) — turning it on notifies the client team (`notifyClientTask`). Client-visible tasks surface in a **"Your tasks"** section on the portal project page.
- **Not yet live-verified on the client side** (needs a real CLIENT login). Internal side verified (`tsc` green). Needs `npx prisma db push` (local) / redeploy (prod) for `Department.clientFacing` + `Task.clientVisible`.

### Companion browser extension (new, 2026-06-16) — localhost build
- **`extension/`** — a plain **MV3 extension (no build step)** that docks the
  user's **To Do / In Progress** tasks to a screen edge with a live timer, an
  expandable **checklist** (toggle) + **related KB**, and ▶/⏸/■ controls. Dock
  position (left/right/top/bottom), collapse, on/off, light/dark theme; Shadow-DOM
  isolated (reads nothing from host pages). Load unpacked from `extension/` — see
  `extension/README.md`.
- **First JSON API** — `app/api/ext/v1/**` route handlers (the app was 100%
  Server Actions before). Auth = revocable **bearer token** (`ExtensionToken`,
  SHA-256, 90-day) via a web-authorize connect flow (`/connect-extension` +
  `launchWebAuthFlow`, no password in the extension) or an email+pw fallback.
  `proxy.ts` exempts `/api/ext/*`. Endpoints: `/me`, `/auth/login`,
  `/auth/revoke`, `/tasks/active`, `/tasks/[id]/timer`, `/checklist/[itemId]`.
  Timer/checklist logic is shared with the web via session-agnostic cores
  (`lib/timer/core.ts`, `lib/projects/task-access.ts`). Manage devices at
  `/profile/devices`. **Localhost-first; production is a config swap.** Live
  browser test is the remaining step.

## Key conventions / decisions
- **Multi-tenant**: every tenant row carries `companyId`; all queries scope by it; server actions re-check ownership.
- **No UI libraries** — charts, Markdown renderer, combobox, datepicker, modal, WYSIWYG, toast, confirm dialog all hand-rolled. **Profile avatars + the company logo upload to disk** (reserved `Employee.photoKey` / `Company.logoKey`, served via scoped `/api/people/[id]/avatar` + `/api/org/logo`, route URL stored in the existing `avatarUrl`/`logoUrl` so render sites are unchanged; shared `ImageUpload` component). KB article images remain URL-in-markdown. Avatars fall back to **first+last-name initials**.
- **UI feedback**: use `toast` (`@/components/ui/toast`) + `confirmDialog` (`@/components/ui/confirm`) — never native `alert()`/`confirm()`.
- **Times**: company-local wall-clock; 12-hour display; "today" via company timezone (`nowInZone`). **Money**: integer paise.
- **Payroll**: statutory rates live in `lib/payroll/config.ts` (config, not magic numbers); calc is pure; company-level PF/ESI toggles; payslips are immutable frozen snapshots; LOP per-day = Gross ÷ 30.
- **Notifications**: taxonomy/colors/deep-links in `lib/notifications/categories.ts` (isomorphic).
- **Task visibility**: per-role scope (`ALL`/`TEAM`/`OWN`) as `task:scope:*` rows in `RolePermission`.
- **Service hierarchy**: `Service.parentId` self-relation — `null` = category (top-level, carries `departmentId`), set = sub-category (inherits the parent's `departmentId`). **Projects link categories** (`ProjectService` → a category); **tasks carry a sub-category** (`Task.serviceId` → a sub-category) which seeds the checklist and scopes assignees to its department. KB articles attach at the sub-category level. Org "General (Tech)/(Design)" categories were created by migration to re-home pre-hierarchy services.
- **Uploads on disk**: `uploads/` at repo root (gitignored); never store bytes in Postgres. Traversal-guarded keys (`lib/uploads.ts`); serving is auth + company-scoped. `Attachment` is polymorphic (`taskId` OR `projectId`); the `AttachmentsPanel` component is shared, parameterized by upload endpoint.

## Pending / what's left
- **Auth & security:**
  - Forgot-password / self-service reset — **✅ built** (`/forgot-password` → reset email → reuses `/set-password`; rides the existing `setupToken` mechanism, 1-hour expiry, no account enumeration; no schema change).
  - Active-session management — needs a `User.tokenVersion` column ("sign out everywhere") or a `Session` table (per-device list).
  - **SSO / Google — deferred until the site is live** (needs Google OAuth creds + the production redirect URL).
- **Audit trail** for sensitive actions (payroll, permission edits, deletions) + viewer — scoped (extends `ActivityLog`).
- **Data export/import** — bulk employee CSV import, company data export (GDPR/portability), soft-delete recovery (`deletedAt` trash/restore) — scoped.
- **Form Builder** — shipped: builder, per-form access, fill, entries table + CSV, portal, 17 field types incl. **date-range / dynamic-list / repeater / calculation**, **conditional visibility**, **auto-calculation** (see Modules live). **Remaining:** charts on entries, form templates/duplicate, entry edit, file-upload fields, submit notifications.
- **File uploads** — **task & project attachments now land on local disk** (`uploads/`, gitignored; `lib/uploads.ts`; POST `/api/tasks/[id]/attachments` + `/api/projects/[id]/attachments`; auth-gated GET `/api/files/[id]`; 100 MB/file cap; `Attachment` carries `taskId` OR `projectId`; bytes never in the DB; shared `AttachmentsPanel` UI). **On a multi-instance host this needs a shared volume or object storage (Supabase Storage / presigned).** Profile avatars + company logo are also on disk now (reserved `photoKey`/`logoKey`). Still unwired elsewhere: employee docs, payslip PDFs, file deliverables.
- **Scheduled jobs / cron** — **✅ endpoint built**: secured `GET/POST /api/cron` (`CRON_SECRET`, proxy-exempt) runs day-before reminders + late-login notices across all companies (`lib/cron/jobs.ts`, idempotent). **Still TODO: wire an external scheduler** (Windows Task Scheduler / system cron / cron-job.org / platform cron) to hit it ~hourly. The lazy page-load triggers remain as a fallback.
- **Timesheet views/approval (Module 7)** — timer creates PENDING `TimeEntry` rows; no approval UI yet.
- **Automated tests** — none (isolation/permission tests especially).
- **Client Portal** — client-side live walkthrough (needs a real CLIENT login).
- **Payroll deferrals:** TDS/income-tax, real PDF-to-storage (printable for now).
- **Resource Allocation** — **frontend pulled (2026-06-16)** at the user's request; **backend scaffold retained** (`lib/resource/{data,actions}.ts` + the `EmployeeCapacity` model). To restore: re-create `app/(app)/resource/page.tsx` (+ a client manager) and re-add the nav entry in `lib/nav.ts`.

## Go-live readiness (2026-06-15)
- **`next build`** was green at 45 routes; now **44** after removing the Resource Allocation page (`/resource`) — source TypeScript clean (`tsc` green; the route-type validator regenerates on the next build). All routes are **dynamic/server-rendered** → deploy to a **Node host** (not static export). The 29 react-hooks lint warnings do **not** block the build.
- **Before flipping live:** set host env vars (`DATABASE_URL`, `DIRECT_URL`, a fresh `AUTH_SECRET`, `SMTP_*`) and **`APP_URL` = the live domain** (invite/reset email links depend on it); clean test artifacts from the shared DB (a June payroll draft run, seeded KB articles, placeholder logo); confirm the company's PF/ESI toggle state (**PF was toggled OFF during LOP testing**).
- **Recommended before real users:** wire the `/api/cron` scheduler; walk the Client Portal client-side end-to-end with a real CLIENT login. (Forgot-password reset is now built.)
- **Pre-launch security pass (2026-06-16):** parallel module audit done; the 🔴 blockers are **fixed** — stored-XSS (KB markdown quote-escape + href; task `finalLink` http(s)-validated at store + `safeHref` at render; uploaded SVG neutralized via CSP `sandbox`+`nosniff` on serve routes + SVG rejected for avatars/logos), leave **self-approval** (can't approve own; HR step ≠ manager), salary-report **over-exposure** (`/reports/payroll` + the payroll KPI now require `payroll:manage`), and **immediate offboarding** (`getSession` re-checks `isActive` + reads current role, so deactivation/role-change apply at once). Remaining 🟡 fast-follow (not blockers): finalize timers on Kanban-move / multi-assignee / unassign; case-insensitive email on login+reset; manager-created leave balance check + duplicate/overlap guard; overnight punch-out; and (before the **extension** ships) CORS fail-closed + `/auth/login` rate-limit.

## Workflow caveats (Windows)
- Dev server on **port 3000**; restart after **schema changes** (`npm run db:push`) and after **renaming/removing an exported symbol** (Turbopack stale-module error). Stop it before `db:push`/`next build` (it holds the engine DLL + `.next` → EPERM).
- Don't panic at a `prisma:error … ConnectionReset (Os 10054)` **burst right after force-killing `next dev`** — it orphans Supabase session-pooler connections; the next server collides until they're reaped (~a minute). Self-heals; pages still 200. Not a connection-string bug.
- Preview tooling: full-page **screenshots often time out** → verify via accessibility/DOM `eval`; viewport **resets narrow on restart** → `preview_resize` ~1680. A server-action form needs `form.requestSubmit()` to submit programmatically (a synthetic submit-button `.click()` won't).
- React **hydration-mismatch warnings** in dev logs are from the **Bitdefender browser extension** (`bis_skin_checked`), not the app.
- Auto-mode classifier **blocks creating external accounts / sending real emails** and **schema migrations on the shared DB without explicit user OK** — those need confirmation.
