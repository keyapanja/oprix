@AGENTS.md

## Project progress (read these first on a fresh session)
- **`docs/STATUS.md`** — current build state: what's live, conventions, what's pending.
- **`docs/REFERENCE.md`** — local setup, architecture & design rationale, build roadmap, and the companion-extension design (one consolidated reference).

Update **`docs/STATUS.md`** at the end of a working session so progress carries across sessions. Keep it current in place — do **not** create per-session `SESSION-<date>.md` files (git history is the change log).
